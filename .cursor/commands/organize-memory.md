# Atlas整理（重複統合・タグ/リンク整備）

## Description
`Atlas/` のノートを重複なく整理し、MOCから辿れる状態に整えます。

## Prompt
`Atlas/` 内のノートを対象に、次を実施してください。

- 近い内容のノートがあれば統合案を提示（破壊的変更は必ず確認）
- frontmatterの `tags` を最小で整備
- 重要ノートはMOCへリンク（最低1リンク）
- `Atlas/` はフラット運用（サブフォルダは作らない）

注意:
- 大量の移動/削除はユーザー確認必須
